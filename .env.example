# 环境变量配置示例
# 复制此文件为 .env 并修改为你自己的配置

# ============================================
# 模型配置
# ============================================

# 单模态模型文件路径
# 支持 .pth (PyTorch), .onnx (ONNX), .rknn (RKNN) 格式
MODEL_SINGLE_PATH=models/single_model.pth

# 融合模态模型文件路径  
# 支持 .pth (PyTorch), .onnx (ONNX), .rknn (RKNN) 格式
MODEL_FUSION_PATH=models/fusion_model.pth

# ============================================
# 服务器配置
# ============================================

# 服务器监听地址
# 127.0.0.1 - 仅本地访问（开发环境）
# 0.0.0.0   - 允许外部访问（生产环境）
HOST=127.0.0.1

# 服务器监听端口
PORT=8000

# ============================================
# 日志配置
# ============================================

# 日志级别
# DEBUG    - 详细调试信息（开发环境）
# INFO     - 一般信息（推荐）
# WARNING  - 警告信息
# ERROR    - 错误信息
# CRITICAL - 严重错误
LOG_LEVEL=INFO

# ============================================
# 推理配置
# ============================================

# 推理设备
# cuda - 使用 GPU（需要 CUDA 支持）
# cpu  - 使用 CPU
DEVICE=cuda

# 模型输入尺寸
# 输入图像将被调整为该尺寸（正方形）
# 默认: 112
INPUT_SIZE=112

# ============================================
# 配置说明
# ============================================
# 
# 1. 复制此文件：
#    cp .env.example .env
#
# 2. 修改 .env 中的值
#
# 3. 环境变量优先级高于 .env 文件：
#    export DEVICE=cpu
#    python main.py
#
# 4. 如果 .env 文件不存在，将使用上述默认值
#
# ============================================
